name: Sherlock Holmes PR Reviewer 🕵️‍♂️

on:
  pull_request:
    types: [opened, edited, synchronize, reopened]

jobs:
  ai_review:
    name: "Sherlock Holmes & Dr. Watson Investigam"
    runs-on: ubuntu-latest
    permissions:
      pull-requests: write
      contents: read

    steps:
      - name: 🧩 Checkout do Código
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: � Verificar Secret da OpenAI
        run: |
          if [ -z "${{ secrets.OPENAI_API_KEY }}" ]; then
            echo "❌ ERRO: Secret OPENAI_API_KEY não está configurado!"
            echo ""
            echo "Para configurar:"
            echo "1. Vá em Settings → Secrets and variables → Actions"
            echo "2. Clique em 'New repository secret'"
            echo "3. Nome: OPENAI_API_KEY"
            echo "4. Value: sua chave da OpenAI (começa com sk-)"
            echo ""
            echo "Obtenha sua chave em: https://platform.openai.com/api-keys"
            exit 1
          else
            echo "✅ Secret OPENAI_API_KEY encontrado!"
          fi

      - name: �🔎 Coletar informações da PR
        id: pr_info
        run: |
          echo "Coletando informações da PR #${{ github.event.pull_request.number }}..."

          # Salvar título
          echo "TITLE<<EOF" >> $GITHUB_ENV
          echo "${{ github.event.pull_request.title }}" >> $GITHUB_ENV
          echo "EOF" >> $GITHUB_ENV

          # Salvar descrição
          echo "BODY<<EOF" >> $GITHUB_ENV
          echo "${{ github.event.pull_request.body }}" >> $GITHUB_ENV
          echo "EOF" >> $GITHUB_ENV
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: 📝 Baixar Diff da Pull Request
        id: diff
        run: |
          echo "Coletando diff da PR..."
          gh pr diff ${{ github.event.pull_request.number }} > diff.txt

          # Limitar o tamanho do diff para não exceder limites da API
          if [ $(wc -c < diff.txt) -gt 50000 ]; then
            echo "DIFF<<EOF" >> $GITHUB_ENV
            head -c 50000 diff.txt >> $GITHUB_ENV
            echo -e "\n\n... (diff truncado por limite de tamanho)" >> $GITHUB_ENV
            echo "EOF" >> $GITHUB_ENV
          else
            echo "DIFF<<EOF" >> $GITHUB_ENV
            cat diff.txt >> $GITHUB_ENV
            echo "EOF" >> $GITHUB_ENV
          fi
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: 🧠 Análise com IA (Sherlock & Watson)
        id: sherlock_analysis
        run: |
          echo "Enviando análise para a IA..."

          # Criar script Python para gerar o payload JSON
          cat > create_payload.py << 'PYTHON_EOF'
          import json
          import os
          import sys

          title = os.environ.get('TITLE', '')
          body = os.environ.get('BODY', '')
          diff = os.environ.get('DIFF', '')

          system_prompt = """Você é **Sherlock Holmes**, o famoso detetive do século XIX, e seu parceiro **John Watson** está ao seu lado. Juntos vocês recebem pull requests para investigar.

          Seu trabalho é analisar cada PR com atenção, examinando:
          - A descrição da PR (se está bem detalhada, clara, contextualizada);
          - O diff dos arquivos modificados;
          - Se está faltando testes ou tipos TypeScript;
          - Se o código segue padrões modernos e clean code, simplicidade e clareza.

          Vocês devem retornar um relatório que contenha:
          1. Uma saudação cordial ("Caro desenvolvedor," ou algo no estilo Sherlock/Watson);
          2. Apreciação pelo esforço ("Obrigado por submeter este código," etc.);
          3. Avaliação da descrição da PR: pontos fortes e onde melhorar;
          4. Análise do código modificado: elogios, achados, alertas se faltar tests ou TypeScript;
          5. Sugestões de melhoria concretas, sempre que possível;
          6. Se tudo estiver em ordem, elogio final;
          7. Se algo estiver faltando (ex: sem testes ou TS), indicar que o PR **não poderá ser aprovado** até que isso seja corrigido.

          O tom deve ser educado, com leve humor dedutivo, e referências ao tema (Sherlock & Watson) são bem-vindas ("Watson notou que…", "Holmes acredita que…"). Use linguagem natural e narrativa envolvente, mas mantenha clareza técnica. Evite respostas excessivamente longas, foque em insights úteis para o desenvolvedor."""

          user_prompt = f"""Aqui está a Pull Request para investigação:

          ## TÍTULO DA PR:
          {title}

          ## DESCRIÇÃO DA PR:
          {body if body else '(Sem descrição fornecida)'}

          ## DIFF DA PR:
          ```diff
          {diff}
          ```

          Por favor, realize a investigação completa e forneça o relatório conforme as instruções acima."""

          payload = {
              "model": "gpt-4o",
              "messages": [
                  {
                      "role": "system",
                      "content": system_prompt
                  },
                  {
                      "role": "user",
                      "content": user_prompt
                  }
              ],
              "temperature": 0.7,
              "max_tokens": 2000
          }

          print(json.dumps(payload, ensure_ascii=False, indent=2))
          PYTHON_EOF

          # Gerar o payload JSON
          python3 create_payload.py > payload.json

          # Fazer a chamada para a API da OpenAI
          RESPONSE=$(curl -s https://api.openai.com/v1/chat/completions \
            -H "Content-Type: application/json" \
            -H "Authorization: Bearer ${{ secrets.OPENAI_API_KEY }}" \
            -d @payload.json)

          # Salvar a resposta completa para debug
          echo "$RESPONSE" > response.json

          # Criar script Python para extrair a análise
          cat > extract_analysis.py << 'PYTHON_EOF'
          import json
          import sys

          try:
              with open('response.json', 'r') as f:
                  response = json.load(f)
              
              # Debug: mostrar resposta completa
              print("=== DEBUG: Resposta da API ===", file=sys.stderr)
              print(json.dumps(response, indent=2), file=sys.stderr)
              print("=" * 50, file=sys.stderr)
              
              if 'error' in response:
                  error_msg = response['error'].get('message', 'Erro desconhecido')
                  error_type = response['error'].get('type', 'unknown')
                  print(f"❌ Erro da API OpenAI ({error_type}): {error_msg}")
                  
                  # Mensagens de ajuda específicas
                  if 'invalid_api_key' in error_type or 'authentication' in error_msg.lower():
                      print("\n💡 Dica: Verifique se o secret OPENAI_API_KEY está configurado corretamente.")
                      print("   A chave deve começar com 'sk-' e estar ativa.")
                  elif 'insufficient_quota' in error_type or 'quota' in error_msg.lower():
                      print("\n💡 Dica: Sua conta OpenAI não tem créditos suficientes.")
                      print("   Adicione créditos em: https://platform.openai.com/account/billing")
                  elif 'model_not_found' in error_type:
                      print("\n💡 Dica: O modelo 'gpt-4o' pode não estar disponível para sua conta.")
                      print("   Tente usar 'gpt-4o-mini' ou 'gpt-3.5-turbo'.")
                  
                  sys.exit(1)
              
              if 'choices' not in response or len(response.get('choices', [])) == 0:
                  print("❌ Resposta da API não contém análise")
                  print(f"Resposta recebida: {json.dumps(response, indent=2)}")
                  sys.exit(1)
              
              analysis = response.get('choices', [{}])[0].get('message', {}).get('content', '')
              
              if not analysis:
                  print("❌ Análise vazia retornada pela API")
                  sys.exit(1)
              
              print(analysis)
              
          except json.JSONDecodeError as e:
              print(f"❌ Erro ao decodificar JSON: {str(e)}")
              print("\n💡 Dica: A resposta da API não está em formato JSON válido.")
              print("Isso geralmente indica um problema de conexão ou erro no servidor da OpenAI.")
              with open('response.json', 'r') as f:
                  print(f"\nResposta raw: {f.read()[:500]}...")
              sys.exit(1)
          except FileNotFoundError:
              print("❌ Arquivo response.json não encontrado")
              sys.exit(1)
          except Exception as e:
              print(f"❌ Erro ao processar resposta: {str(e)}")
              import traceback
              traceback.print_exc()
              sys.exit(1)
          PYTHON_EOF

          # Extrair a análise
          ANALYSIS=$(python3 extract_analysis.py 2>&1)
          EXIT_CODE=$?

          # Verificar se houve erro
          if [ $EXIT_CODE -ne 0 ]; then
            echo ""
            echo "=========================================="
            echo "❌ FALHA NA ANÁLISE DA IA"
            echo "=========================================="
            echo ""
            echo "$ANALYSIS"
            echo ""
            echo "=========================================="
            echo "📋 Resposta completa da API:"
            echo "=========================================="
            cat response.json
            exit 1
          fi

          # Salvar análise para o próximo step
          echo "ANALYSIS<<EOF" >> $GITHUB_ENV
          echo "$ANALYSIS" >> $GITHUB_ENV
          echo "EOF" >> $GITHUB_ENV

          echo "✅ Análise concluída com sucesso!"
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          TITLE: ${{ env.TITLE }}
          BODY: ${{ env.BODY }}
          DIFF: ${{ env.DIFF }}

      - name: 💬 Comentar na PR
        if: success()
        run: |
          echo "Publicando análise na PR..."

          # Criar comentário formatado
          cat > comment.md << 'COMMENT_EOF'
          ## 🕵️‍♂️ Investigação de Sherlock Holmes & Dr. Watson

          ${{ env.ANALYSIS }}

          ---
          *Análise automática gerada por IA • Sherlock Holmes PR Reviewer*
          COMMENT_EOF

          # Postar comentário
          gh pr comment ${{ github.event.pull_request.number }} --body-file comment.md
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: ⚠️ Tratamento de Erros
        if: failure()
        run: |
          echo "Houve um erro durante a análise. Verifique os logs acima."
          gh pr comment ${{ github.event.pull_request.number }} --body "⚠️ **Atenção**: A análise automática de Sherlock Holmes encontrou um problema técnico. Por favor, revise manualmente esta PR."
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
